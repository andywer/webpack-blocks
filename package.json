{
  "private": true,
  "scripts": {
    "test": "yarn prettier --list-different && yarn lint && lerna run test",
    "lint": "eslint \"**/*.js\"",
    "format": "yarn prettier --write",
    "prettier": "prettier \"**/*.{js,ts,tsx}\" --loglevel warn"
  },
  "devDependencies": {
    "ava": "^0.22.0",
    "eslint": "^5.0.0-alpha.2",
    "eslint-config-prettier": "^2.9.0",
    "lerna": "^2.10.2",
    "prettier": "^1.12.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "printWidth": 100,
    "semi": false,
    "singleQuote": true
  }
}
