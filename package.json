{
  "private": true,
  "scripts": {
    "test": "lerna run --parallel test",
    "postinstall": "lerna bootstrap --hoist && link-parent-bin"
  },
  "devDependencies": {
    "ava": "^0.22.0",
    "lerna": "2.4.0",
    "link-parent-bin": "^0.1.3",
    "standard": "^10.0.3"
  }
}
